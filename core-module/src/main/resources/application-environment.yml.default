APPLICATION:
  SERVER:
    PORT: ${APPLICATION_SEVER_PORT}

DATABASE:
  HOST: ${DATABASE_HOST}
  PORT: ${DATABASE_PORT}
  NAME: ${DATABASE_NAME}
  USERNAME: ${DATABASE_USERNAME}
  PASSWORD: ${DATABASE_PASSWORD}
